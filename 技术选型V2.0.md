# 混凝土ERP系统架构选型方案（完整版）

## 一、总体架构概览

采用**分层、可演进的单体外加模块化设计**。初期作为单体应用部署，但内部模块高度解耦，未来可平滑拆分为微服务。

text

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│   Web端 (PC)  │  移动端 (H5/小程序)  │  工控机终端          │
└────────────────────────┬────────────────────────────────────┘
                         │  HTTPS / WSS
┌────────────────────────▼────────────────────────────────────┐
│                      接入层 (Nginx)                          │
│         反向代理  │  负载均衡  │  SSL卸载  │  限流           │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    API 网关层                                │
│     认证鉴权  │  路由转发  │  协议转换  │  日志记录         │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    应用服务层                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ 经营合同  │ │ 生产调度  │ │ 技术质量  │ │ 物资库存     │  │
│  │ 服务     │ │ 服务     │ │ 服务     │ │ 服务         │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ 结算财务  │ │ 设备管理  │ │ 报表服务  │ │ 消息通知     │  │
│  │ 服务     │ │ 服务     │ │          │ │ 服务         │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              系统基础服务层                            │  │
│  │  租户管理 │ 用户认证 │ 权限管理 │ 日志审计 │ 定时任务  │  │
│  │  文件存储 │ 消息队列 │ 配置中心 │ 租户脚本引擎        │  │
│  │  协作域引擎│ 公告服务 │ 许可证管理                     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    数据层                                    │
│  ┌────────────┐  ┌────────────┐  ┌────────────────────┐    │
│  │ SQL Server │  │   Redis    │  │  MinIO / 阿里云OSS │    │
│  │ (主数据库) │  │ (缓存/会话) │  │ (文件/报告存储)    │    │
│  └────────────┘  └────────────┘  └────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    边缘层 (站内工控机)                        │
│  数据网关 │ PLC通讯 │ 本地缓存 │ 断网续传 │ 脱机生产保障    │
└─────────────────────────────────────────────────────────────┘
```



------

## 二、多租户与跨站协作架构

系统采用**"租户为基座，四种机制各司其职"**的架构模式，以应对两极分化的市场：

- **极小型客户**：3-5人团队运营一个小型搅拌站，使用单租户模式，无跨站概念
- **集团大客户**：多个站点需要集中调度、集采、报表审阅、信息共享

### 2.1 四种机制总览

| 机制                            | 业务场景               | 数据流向     | 权限类型  | 适用客户 |
| :------------------------------ | :--------------------- | :----------- | :-------- | :------- |
| **租户（Tenant）**              | 独立站点日常经营       | 站内闭环     | 完整操作  | 所有客户 |
| **组织层级（Parent-Child）**    | 集团报表审阅           | 单向向上汇总 | 只读穿透  | 集团客户 |
| **协作域（Collaboration Hub）** | 跨站集中调度、集中采购 | 双向协同     | 操作共享  | 集团客户 |
| **公告与知识库**                | 集团信息下发、共享资料 | 单向向下广播 | 发布-订阅 | 集团客户 |

### 2.2 租户设计（Tenant）

**适用场景**：所有搅拌站都必须是一个租户。极小型客户只有一个租户，集团客户每个搅拌站各自是一个租户。

**设计要点**：

- 所有业务表包含 `TenantId` 字段，EF Core 全局查询筛选器自动注入 `WHERE TenantId = @currentTenantId`
- 通过 `Sys_TenantLicenses` 表管理功能包购买与开通
- 极小型客户登录后只看到自己的站点，不显示任何跨站菜单
- 大客户根据已购许可证包展示对应功能

**数据库核心表**：

sql

```
-- 租户表
CREATE TABLE Sys_Tenants (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    TenantCode      VARCHAR(20)  NOT NULL UNIQUE,
    TenantName      NVARCHAR(100) NOT NULL,
    TenantType      TINYINT NOT NULL DEFAULT 1,      -- 1=单站 2=集团
    ParentTenantId  INT NULL,                        -- 上级集团ID（用于组织层级）
    Status          TINYINT NOT NULL DEFAULT 1,
    RegionCode      VARCHAR(10) NULL,
    ExpireDate      DATETIME2 NULL,
    TenantId        INT NOT NULL DEFAULT 0,          -- 0=系统级数据
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    CreatedBy       INT NOT NULL,
    UpdatedBy       INT NOT NULL,
    IsDeleted       BIT NOT NULL DEFAULT 0
);

-- 许可证包定义
CREATE TABLE Sys_LicensePackages (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    PackageCode     VARCHAR(50) NOT NULL UNIQUE,     -- 'central_dispatch' | 'procurement' | 'report'
    PackageName     NVARCHAR(100) NOT NULL,
    Category        VARCHAR(30) NOT NULL,            -- 'basic' | 'advanced' | 'premium'
    DefaultGranted  BIT NOT NULL DEFAULT 0,
    TenantId        INT NOT NULL DEFAULT 0,
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    IsDeleted       BIT NOT NULL DEFAULT 0
);

-- 租户已购许可证
CREATE TABLE Sys_TenantLicenses (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    TenantId        INT NOT NULL,
    PackageCode     VARCHAR(50) NOT NULL,
    StartDate       DATE NOT NULL,
    EndDate         DATE NOT NULL,
    IsActive        BIT NOT NULL DEFAULT 1,
    GrantedBy       INT NOT NULL,
    GrantedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    IsDeleted       BIT NOT NULL DEFAULT 0,
    UNIQUE INDEX UQ_TenantLicense (TenantId, PackageCode)
);
```



**功能包清单**：

| 功能包代码            | 名称       | 极小型客户 | 集团客户 |
| :-------------------- | :--------- | :--------- | :------- |
| `basic_operation`     | 基础运营包 | ✅ 必选     | ✅ 必选   |
| `material_mgmt`       | 物资管理包 | ✅ 必选     | ✅ 必选   |
| `finance`             | 结算财务包 | ✅ 必选     | ✅ 必选   |
| `equipment`           | 设备车辆包 | ⬜ 可选     | ✅ 必选   |
| `central_dispatch`    | 集中调度包 | ⬜ 不可见   | ✅ 按需   |
| `central_procurement` | 集采中枢包 | ⬜ 不可见   | ✅ 按需   |
| `advanced_report`     | 高级报表包 | ⬜ 可选     | ✅ 必选   |
| `regulatory_report`   | 监管上报包 | ✅ 必选     | ✅ 必选   |

### 2.3 组织层级设计（Parent-Child Hierarchy）

**适用场景**：集团财务总监查看所有子公司应收应付汇总报表，集团技术总工审阅各站点配合比使用情况，集团总经理查看各站点生产日报。

**为什么独立于协作域**：报表审阅是上下级关系的只读查看，协作域是对等关系的双向操作，权限模型完全不同，必须分离。

**设计要点**：

- 利用 `Sys_Tenants.ParentTenantId` 建立父子关系
- 通过 `Sys_ReportReviewPermissions` 表精细化控制审阅权限
- 可限制某人只能看"财务报表"而不能看"生产配比"
- 子公司日常操作不受集团查看影响

**数据库核心表**：

sql

```
-- 报表审阅权限表
CREATE TABLE Sys_ReportReviewPermissions (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    UserId          INT NOT NULL,
    ParentTenantId  INT NOT NULL,                     -- 集团ID
    ChildTenantId   INT NOT NULL,                     -- 可审阅的子公司ID
    ReviewScope     VARCHAR(50) NOT NULL,             -- 'All' | 'Finance' | 'Production' | 'Quality'
    IsActive        BIT NOT NULL DEFAULT 1,
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    CreatedBy       INT NOT NULL,
    INDEX IX_ReportReview_User (UserId),
    UNIQUE INDEX UQ_ReportReview (UserId, ParentTenantId, ChildTenantId, ReviewScope)
);
```



**JWT Token 扩展**：

json

```
{
  "sub": "user_zhang",
  "report_review": {
    "parent_tenant_id": 100,
    "reviewable_tenants": [
      {"tenant_id": 1, "scope": "Finance"},
      {"tenant_id": 2, "scope": "Finance"}
    ]
  }
}
```



**后端查询实现**：

csharp

```
// 集团报表服务
public async Task<GroupFinanceReport> GetGroupFinanceReport(int userId, int parentTenantId)
{
    var childTenantIds = await _dbContext.Sys_ReportReviewPermissions
        .Where(p => p.UserId == userId && p.ParentTenantId == parentTenantId 
                 && p.ReviewScope == "Finance" && p.IsActive)
        .Select(p => p.ChildTenantId)
        .ToListAsync();
    
    return await _dbContext.FinanceRecords
        .IgnoreQueryFilters()  // 临时禁用单租户过滤
        .Where(r => childTenantIds.Contains(r.TenantId))
        .GroupBy(r => r.TenantId)
        .Select(g => new TenantFinanceSummary { ... })
        .ToListAsync();
}
```



### 2.4 协作域设计（Collaboration Hub）

**适用场景**：

- **集中调度域**：京西片区下A、B、C三个站成立集中调度中心，调度员可同时查看和操作三个站的任务和车辆
- **集采中枢域**：北京地区所有站点统一采购水泥，共享供应商和合同
- **灵活租站**：新租站点加入调度域即纳入统一管理，退租后移除

**为什么独立设计**：协作域是对等成员间的双向操作协同，与组织层级的上下级只读审阅完全不同。一个站点可同时属于多个域。

**数据库核心表**：

sql

```
-- 协作域定义
CREATE TABLE Col_CollaborationHubs (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    HubCode         VARCHAR(30) NOT NULL UNIQUE,
    HubName         NVARCHAR(100) NOT NULL,
    HubType         VARCHAR(30) NOT NULL,             -- 'CentralDispatch' | 'CentralProcurement'
    OwnerTenantId   INT NOT NULL,
    RegionCode      VARCHAR(10) NULL,
    Status          TINYINT NOT NULL DEFAULT 1,
    ConfigJson      NVARCHAR(MAX) NULL,
    TenantId        INT NOT NULL,
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    CreatedBy       INT NOT NULL,
    UpdatedBy       INT NOT NULL,
    IsDeleted       BIT NOT NULL DEFAULT 0
);

-- 协作域成员
CREATE TABLE Col_TenantHubMembers (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    TenantId        INT NOT NULL,
    HubId           INT NOT NULL,
    JoinType        VARCHAR(20) NOT NULL DEFAULT 'FullMember', -- 'FullMember' | 'ViewOnly'
    JoinedAt        DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    LeftAt          DATETIME2 NULL,                  -- 退出时间
    IsActive        BIT NOT NULL DEFAULT 1,
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    IsDeleted       BIT NOT NULL DEFAULT 0,
    INDEX IX_TenantHubMembers_HubId (HubId),
    UNIQUE INDEX UQ_TenantHub (TenantId, HubId)
);
```



**用户角色关联**：

sql

```
-- 在 Sys_UserRoleAssignments 中支持 ScopeType = 'Hub'
CREATE TABLE Sys_UserRoleAssignments (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    UserId          INT NOT NULL,
    RoleCode        VARCHAR(50) NOT NULL,             -- 'HubScheduler' | 'HubProcurement'
    ScopeType       VARCHAR(20) NOT NULL DEFAULT 'Tenant', -- 'Tenant' | 'Hub'
    ScopeId         INT NOT NULL,                     -- TenantId 或 HubId
    GrantedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    GrantedBy       INT NOT NULL,
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    IsDeleted       BIT NOT NULL DEFAULT 0,
    INDEX IX_UserRoleAssignments_UserId (UserId),
    UNIQUE INDEX UQ_UserRoleScope (UserId, RoleCode, ScopeType, ScopeId)
);
```



**JWT Token 扩展**：

json

```
{
  "sub": "user_wang",
  "scope_permissions": [
    {
      "type": "Tenant",
      "id": 1,
      "name": "京西A站",
      "role": "ViewOnly"
    },
    {
      "type": "Hub",
      "id": 101,
      "name": "京西集中调度中心",
      "role": "HubScheduler",
      "hubType": "CentralDispatch",
      "accessibleTenants": [1, 2, 3]
    }
  ]
}
```



**API路径约定**：

| API路径                           | 作用域 | 说明       |
| :-------------------------------- | :----- | :--------- |
| `/api/v1/tenant/{tenantId}/tasks` | 单租户 | 站内操作   |
| `/api/v1/hub/{hubId}/tasks`       | 协作域 | 跨站协同   |
| `/api/v1/hub/{hubId}/vehicles`    | 协作域 | 统一车辆池 |

**前端实现要点**：

- 顶栏上下文切换器：`🔽 京西集中调度中心`
- 切换到协作域后显示专属协同工作台
- 数据列表用颜色标签标注来源站点
- 请求携带 `X-Scope-Type: Hub` 和 `X-Scope-Id: 101`

### 2.5 公告与知识库设计

**适用场景**：集团发布供应商名录更新、施工规范调整、安全培训通知等，所有子公司按需接收和查看。

**为什么独立设计**：一对多广播，内容管理属性（时效性、已读追踪），与协作域的双向协同完全不同。

**数据库核心表**：

sql

```
-- 公告表
CREATE TABLE Info_Announcements (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    PublisherTenantId INT NOT NULL,
    Title           NVARCHAR(200) NOT NULL,
    Content         NVARCHAR(MAX) NULL,
    Category        VARCHAR(50) NOT NULL,             -- 'Notice' | 'Policy' | 'Training' | 'SupplierInfo'
    Priority        TINYINT NOT NULL DEFAULT 0,       -- 0=普通 1=重要 2=紧急
    IsPinned        BIT NOT NULL DEFAULT 0,
    PublishAt       DATETIME2 NOT NULL,
    ExpireAt        DATETIME2 NULL,
    TenantId        INT NOT NULL,
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    CreatedBy       INT NOT NULL,
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedBy       INT NOT NULL,
    IsDeleted       BIT NOT NULL DEFAULT 0
);

-- 公告可见范围
CREATE TABLE Info_AnnouncementTargets (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    AnnouncementId  INT NOT NULL,
    TargetTenantId  INT NOT NULL,
    IsReadRequired  BIT NOT NULL DEFAULT 0,
    INDEX IX_AnnouncementTargets_AnnId (AnnouncementId),
    INDEX IX_AnnouncementTargets_TenantId (TargetTenantId)
);

-- 已读记录
CREATE TABLE Info_AnnouncementReadLogs (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    AnnouncementId  INT NOT NULL,
    UserId          INT NOT NULL,
    TenantId        INT NOT NULL,
    ReadAt          DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UNIQUE INDEX UQ_ReadLog (AnnouncementId, UserId)
);

-- 共享知识库
CREATE TABLE Info_SharedKnowledge (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    PublisherTenantId INT NOT NULL,
    Title           NVARCHAR(200) NOT NULL,
    Content         NVARCHAR(MAX) NULL,
    Category        VARCHAR(50) NOT NULL,             -- 'SupplierList' | 'MixDesign' | 'Process' | 'Regulation'
    AttachmentsJson NVARCHAR(MAX) NULL,
    Version         INT NOT NULL DEFAULT 1,
    TenantId        INT NOT NULL,
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    CreatedBy       INT NOT NULL,
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedBy       INT NOT NULL,
    IsDeleted       BIT NOT NULL DEFAULT 0
);
```



**API设计**：

| 方法 | API路径                                        | 说明                 | 权限       |
| :--- | :--------------------------------------------- | :------------------- | :--------- |
| POST | `/api/v1/info/announcements`                   | 发布公告             | 集团管理员 |
| GET  | `/api/v1/info/announcements`                   | 获取当前租户可见公告 | 所有用户   |
| PUT  | `/api/v1/info/announcements/{id}/read`         | 标记已读             | 所有用户   |
| GET  | `/api/v1/info/announcements/{id}/read-stats`   | 查看已读统计         | 集团管理员 |
| GET  | `/api/v1/info/knowledge?category=SupplierList` | 获取共享知识         | 子公司用户 |

### 2.6 四种机制完整对比

| 维度           | 租户           | 组织层级                    | 协作域                | 公告与知识库       |
| :------------- | :------------- | :-------------------------- | :-------------------- | :----------------- |
| **核心目的**   | 数据隔离与计费 | 集团向下审阅                | 跨站业务协同          | 集团信息下发       |
| **数据流向**   | 站内闭环       | 单向向上汇总                | 双向操作共享          | 单向向下广播       |
| **权限性质**   | 完整操作       | 只读穿透                    | 操作共享              | 发布-订阅          |
| **成员关系**   | 独立单元       | 父子从属                    | 对等成员              | 发布者-订阅者      |
| **变动频率**   | 极少           | 极少                        | 较频繁                | 频繁               |
| **小客户可见** | 是，唯一界面   | 否，菜单隐藏                | 否，菜单隐藏          | 否，菜单隐藏       |
| **核心表**     | Sys_Tenants    | Sys_ReportReviewPermissions | Col_CollaborationHubs | Info_Announcements |

------

## 三、前端技术栈选型

| 选型项         | 推荐方案           | 备选方案                 | 选型理由                                                     |
| :------------- | :----------------- | :----------------------- | :----------------------------------------------------------- |
| **框架**       | **Vue 3**          | React 18                 | 组合式API适合组织复杂逻辑，与"基础模块→集成中心"思路天然契合 |
| **构建工具**   | **Vite**           | Webpack 5                | 开发服务器秒级启动，热更新极快                               |
| **UI组件库**   | **Element Plus**   | Ant Design Vue, Naive UI | 中文社区活跃，表格/表单/弹窗等ERP常用组件成熟                |
| **状态管理**   | **Pinia**          | Vuex 4                   | TypeScript支持好，适合管理多租户、多协作域复杂状态           |
| **路由**       | **Vue Router 4**   | -                        | 配合动态路由，按许可证过滤菜单                               |
| **HTTP客户端** | **Axios**          | -                        | 拦截器封装全局认证、租户头注入、协作域上下文                 |
| **表格组件**   | **Vxe-Table**      | AG Grid                  | 复杂列配置、数据导出、单元格编辑，搅拌站列表页核心           |
| **图表库**     | **ECharts 5**      | AntV G2                  | 报表看板、经营驾驶舱数据可视化                               |
| **微前端**     | **Qiankun** (预留) | Micro-app                | 租户自定义页面预留（远期）                                   |
| **移动端**     | **Uni-app**        | Taro                     | 一套代码编译H5/小程序/APP，老板看报表、调度收通知            |

------

## 四、后端技术栈选型

| 选型项           | 推荐方案                                            | 备选方案                | 选型理由                                               |
| :--------------- | :-------------------------------------------------- | :---------------------- | :----------------------------------------------------- |
| **框架**         | **.NET 8**                                          | Java 17 (Spring Boot 3) | 与SQL Server同属微软生态，EF Core集成体验业界最佳      |
| **ORM**          | **EF Core 8** + Dapper                              | -                       | EF Core用于80%常规业务，Dapper用于20%复杂报表SQL       |
| **认证与授权**   | **[ASP.NET](https://asp.net/) Core Identity + JWT** | IdentityServer          | 支持多租户多域权限，Token携带Scope和可访问租户列表     |
| **多租户支持**   | **EF Core全局查询筛选器**                           | 数据库RLS               | ORM层自动注入tenant_id，协作域场景可IgnoreQueryFilters |
| **API文档**      | **Swagger / Swashbuckle**                           | -                       | 自动生成交互式API文档                                  |
| **定时任务**     | **[Quartz.NET](https://quartz.net/)**               | Hangfire                | 支持Cron，满足定时备份、自动报表、库存预警             |
| **消息队列**     | **RabbitMQ** (初期Redis Pub/Sub)                    | Kafka                   | 初期Redis轻量处理消息通知和异步报表                    |
| **租户脚本引擎** | **Jint** (JavaScript引擎)                           | ClearScript             | 与前端的JS技术栈统一，纯C#实现安全可控                 |
| **实时通讯**     | **SignalR**                                         | -                       | WebSocket支持调度中心实时车辆状态、任务提醒            |

------

## 五、报表与打印方案

### 5.1 报表分层架构

搅拌站ERP的报表需求分为三个层次，每层技术方案不同：

| 层次             | 说明                 | 典型报表                               | 技术方案                       |
| :--------------- | :------------------- | :------------------------------------- | :----------------------------- |
| **站内业务单据** | 单站点日常打印       | 配合比报告、结算单、过磅单、发货单     | FastReport模板 + API填数 → PDF |
| **站内统计报表** | 单站点汇总统计       | 生产日报、材料消耗汇总、应收账款       | FastReport + Dapper直连SQL     |
| **跨站协作报表** | 多站点汇总/对比/穿透 | 集团生产汇总、集采消耗分析、经营驾驶舱 | FastReport + API聚合多租户数据 |

### 5.2 报表引擎选型

| 选型项       | 推荐方案                                         | 说明                                                         |
| :----------- | :----------------------------------------------- | :----------------------------------------------------------- |
| **报表引擎** | **FastReport.OpenSource**                        | .NET生态老牌报表，开源免费，NuGet包 `FastReport.OpenSource`。支持直接绑定DataSet/DataTable，对SQL Server直连友好。支持Excel/Word/PDF/CSV等多种导出格式。支持编码创建模板或预置.frx文件 |
| **模板存储** | 数据库 + 文件存储                                | .frx模板文件存储在MinIO/OSS，数据库中存模板元数据（名称、分类、版本、绑定数据源） |
| **异步生成** | [Quartz.NET](https://quartz.net/)定时 + 消息队列 | 大批量月报生成放入队列异步处理，生成完成后通知用户下载       |

### 5.3 报表模板管理

**数据库表**：

sql

```
-- 报表模板定义表
CREATE TABLE Rpt_Templates (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    TemplateCode    VARCHAR(50) NOT NULL,            -- 'MixRatioReport' | 'SettlementReport'
    TemplateName    NVARCHAR(100) NOT NULL,
    Category        VARCHAR(30) NOT NULL,            -- 'StationBill' | 'StationReport' | 'GroupReport'
    ReportScope     VARCHAR(20) NOT NULL DEFAULT 'SingleTenant', -- 'SingleTenant' | 'MultiTenant' | 'GroupReview'
    FilePath        VARCHAR(500) NOT NULL,           -- MinIO/OSS存储路径
    DataSourceConfig NVARCHAR(MAX) NULL,             -- 数据源配置JSON
    Version         INT NOT NULL DEFAULT 1,
    TenantId        INT NOT NULL,                    -- 0=系统预置模板，>0=租户自定义模板
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    UpdatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    CreatedBy       INT NOT NULL,
    UpdatedBy       INT NOT NULL,
    IsDeleted       BIT NOT NULL DEFAULT 0,
    UNIQUE INDEX UQ_Template (TenantId, TemplateCode)
);

-- 报表生成记录
CREATE TABLE Rpt_GenerationLogs (
    Id              INT IDENTITY(1,1) PRIMARY KEY,
    TemplateId      INT NOT NULL,
    TenantId        INT NOT NULL,
    ReportScope     VARCHAR(20) NOT NULL,            -- 'SingleTenant' | 'MultiTenant'
    ScopeId         INT NULL,                        -- HubId（协作域报表）或 ParentTenantId（集团报表）
    ParametersJson  NVARCHAR(MAX) NULL,              -- 查询参数JSON
    OutputFormat    VARCHAR(10) NOT NULL DEFAULT 'PDF',
    FilePath        VARCHAR(500) NULL,               -- 生成的文件存储路径
    Status          VARCHAR(20) NOT NULL DEFAULT 'Pending', -- 'Pending' | 'Processing' | 'Completed' | 'Failed'
    ErrorMessage    NVARCHAR(MAX) NULL,
    CreatedAt       DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    CreatedBy       INT NOT NULL,
    CompletedAt     DATETIME2 NULL
);
```



### 5.4 报表生成流程

**站内报表（SingleTenant）**：

text

```
用户选择模板 → 前端发起请求（模板ID + 查询条件）
  → 后端校验租户权限
  → 调用Dapper执行SQL查询（自动应用TenantId过滤）
  → FastReport加载.frx模板，绑定DataSet
  → 渲染PDF/Excel
  → 返回文件流给前端
  → 前端PDF.js预览 / 直接下载
```



**跨站报表（MultiTenant / GroupReview）**：

text

```
用户选择模板 + 选择范围（协作域/集团/指定站点）
  → 前端发起请求（模板ID + ScopeType + ScopeId + 查询条件）
  → 后端校验用户对Scope的权限
  → 根据ScopeType获取可访问的TenantId列表
  → 调用Dapper执行SQL查询（IgnoreQueryFilters，IN查询）
  → 数据集中标注来源站点
  → FastReport加载模板，绑定聚合数据
  → 渲染PDF/Excel
  → 返回文件流
```



### 5.5 打印方案

| 环节         | 方案            | 说明                                            |
| :----------- | :-------------- | :---------------------------------------------- |
| **预览**     | PDF.js          | 浏览器内嵌预览，确保格式一致                    |
| **打印**     | PDF静默打印     | 避免浏览器打印导致的格式错乱                    |
| **批量打印** | 后台合并PDF     | 多份结算单、多份试验报告合并为一个PDF，一次打印 |
| **防伪**     | 二维码 + 校验码 | 每份报告生成唯一二维码，扫码可验证真伪          |

------

## 六、数据库选型与设计原则

| 选型项            | 推荐方案                              | 说明                                                         |
| :---------------- | :------------------------------------ | :----------------------------------------------------------- |
| **主数据库**      | **SQL Server 2019+**                  | 团队能力所在。使用RLS作为数据隔离最终防线。列存储索引优化报表。Always On可用性组实现读写分离和故障转移 |
| **缓存与Session** | **Redis**                             | 存储登录会话、常用配置、实时车辆状态等热数据                 |
| **文件存储**      | **MinIO** (自建) / **阿里云OSS** (云) | 合同扫描件、签字图片、试块照片、PDF报告、.frx模板文件        |

**数据库设计核心约定**（所有表）：

- `TenantId`：int，租户隔离主键，**所有业务表必有**
- `RegionCode`：varchar(10)，地区编码
- 使用表前缀区分模块：`Sys_`（系统）、`Col_`（协作）、`Info_`（公告知识库）、`Rpt_`（报表）、`Ctr_`（经营）、`Prd_`（生产）、`Tech_`（技术）、`Biz_`（业务通用）
- 所有表包含 `CreatedAt`、`UpdatedAt`、`CreatedBy`、`UpdatedBy` 审计字段
- 所有删除使用软删除（`IsDeleted`位字段），配合EF Core全局筛选器自动过滤

------

## 七、接口层设计规范

| 选型项       | 推荐方案               | 说明                                                         |
| :----------- | :--------------------- | :----------------------------------------------------------- |
| **API风格**  | **RESTful API**        | 用于前后端交互、第三方系统对接                               |
| **数据格式** | **JSON**               | 标准格式                                                     |
| **版本管理** | `/api/v1/` URL前缀     | 为未来升级预留                                               |
| **错误处理** | 统一错误码体系         | 全局错误码枚举，全局异常过滤器，返回 `{ code, message, data }` |
| **实时通讯** | **SignalR**            | 调度中心实时车辆状态、新任务提醒、消息推送                   |
| **设备接口** | **独立的协议转换网关** | 工控机/地磅硬件接口不直接开放HTTP，通过MQTT或加密HTTP上传    |

**协作域相关Header约定**：

| Header         | 说明       | 示例              |
| :------------- | :--------- | :---------------- |
| `X-Tenant-Id`  | 当前租户ID | `1`               |
| `X-Scope-Type` | 上下文类型 | `Tenant` 或 `Hub` |
| `X-Scope-Id`   | 上下文ID   | `101`（HubId）    |

------

## 八、部署架构建议

| 环境              | 说明                                                         |
| :---------------- | :----------------------------------------------------------- |
| **开发环境**      | 开发人员本地。SQL Server用Docker部署，Redis用Docker          |
| **测试环境**      | 一台低配云服务器，模拟生产环境                               |
| **生产环境-云端** | **应用服务器**：Windows Server / Linux均可，初期4核8G承载30个站。**数据库服务器**：独立服务器跑SQL Server。**文件服务器**：MinIO单实例或OSS |
| **生产环境-站内** | 工控机安装Windows，部署边缘网关服务，本地SQL Server Express做断网缓存 |

------

## 九、开发协作工具建议

| 工具                      | 用途                                    |
| :------------------------ | :-------------------------------------- |
| **Azure DevOps / GitLab** | 源码管理、CI/CD流水线、需求管理         |
| **Docker**                | 统一开发环境，SQL Server和Redis本地部署 |
| **Swagger / Apifox**      | API文档与前后端联调                     |

------

## 十、实施优先级建议

| 阶段        | 内容                         | 说明                                 |
| :---------- | :--------------------------- | :----------------------------------- |
| **Phase 1** | 租户 + 许可证 + 基础业务模块 | 所有功能的基础，必须最先实现         |
| **Phase 2** | 协作域（集中调度）           | 大客户核心需求                       |
| **Phase 3** | 报表系统（站内 + 跨站）      | Phase 1-2产生数据后报表才有意义      |
| **Phase 4** | 组织层级（报表审阅）         | 依赖Phase 1的租户层级和Phase 3的报表 |
| **Phase 5** | 公告与知识库                 | 独立模块，最后实现                   |
| **Phase 6** | 协作域（集采中枢）           | 依赖Phase 2的协作域框架              |

------



# ORM与数据访问层选型补充说明（简要版）

> 本文档是对《混凝土ERP系统架构选型方案》中ORM选型的细化补充。

------

## 一、三种数据访问方式的定位与边界

系统采用 **EF Core + Dapper + DataTable** 三者共存的方案，按场景选择：

| 维度           | EF Core 8           | Dapper                        | DataTable / SqlDataAdapter     |
| :------------- | :------------------ | :---------------------------- | :----------------------------- |
| **核心目标**   | 用对象操作数据库    | 手写SQL + 快速映射已知类型    | 运行时动态列，编译时不知列结构 |
| **适用场景**   | 常规CRUD（80%业务） | 复杂报表、存储过程、固定列SQL | 动态视图查询、多结果集存储过程 |
| **租户隔离**   | 自动（全局筛选器）  | 手动（SQL中加参数）           | 手动（SQL中加参数）            |
| **变更追踪**   | 自动                | 无                            | 无                             |
| **类型安全**   | 编译时检查          | 运行时检查                    | 运行时通过元数据检查           |
| **字段动态性** | 不支持              | 不支持                        | **原生支持**                   |

**选择原则**：

text

```
需要自动租户过滤 + 变更追踪？
  ├── 是 → EF Core
  └── 否 → 列结构编译时已知？
            ├── 是 → Dapper
            └── 否 → DataTable + SqlDataAdapter
```



------

## 二、为什么需要三种方式

### EF Core 的不可替代点

多租户全局筛选器自动注入 `WHERE TenantId = @id`，开发者写代码时不需要手动加租户条件，安全性由框架保证。同时提供 `IgnoreQueryFilters()` 在协作域跨站查询时临时解除隔离。审计字段（CreatedAt、UpdatedBy）在 `SaveChanges` 中自动填充。

### Dapper 的定位

解决手写SQL后"把查询结果自动映射到C#对象"的问题，消灭手动写 `reader.GetInt32(reader.GetOrdinal("..."))` 的体力劳动。用于20%的复杂报表和存储过程调用——这些场景SQL逻辑复杂，手写SQL比LINQ更高效，团队的SQL能力可以得到充分发挥。

### DataTable 的定位

当视图或存储过程返回的列在编译时未知时，DataTable 可以从数据库元数据自动拉取列名和数据类型，不需要定义实体类。视图新增列后不需要改C#代码。

------

## 三、动态映射视图

### 场景

系统中存在大量视图（如`生产任务_V`），关联5-8张基础表，包含几十个字段。视图列可能随业务需求新增或调整。如果为每个视图写实体类，视图每次加列都需要改C#代码、重新编译、重新发布。

### 方案

后端通过 `INFORMATION_SCHEMA.COLUMNS` 查询视图的列元数据（按 `ORDINAL_POSITION` 排序保证顺序），使用 `SqlDataAdapter.Fill(DataTable)` 查询数据，DataTable 自动从数据库拉取列名和数据类型。API同时返回 `columns` 数组和 `rows` 数据，前端 `v-for` 遍历 `columns` 动态渲染表格。

视图新增列后，数据库元数据自动反映变化，后端查询时返回更新后的列信息，前端自动渲染新列，无需任何代码变更。

### 多结果集存储过程

`SqlDataAdapter.Fill(DataSet)` 原生支持多结果集，`dataSet.Tables[0]` 是第一个结果集，`Tables[1]` 是第二个。每个结果集独立提取列元数据，API返回 `resultSets` 数组，前端逐表格渲染，每个表格的列完全由各自结果集的列信息决定。

------

## 四、缓存刷新：事件驱动 + Buffer Pool

### 场景

"生产任务_V"等视图查询频率极高、JOIN表多，但数据变化频率中等。每次都实时查询视图，JOIN开销大。需要缓存机制提升响应速度。

### 方案

**不使用SQL Server物化视图**：因为它不支持LEFT JOIN，而生产任务视图必须用LEFT JOIN（任务可能未分配车辆，车辆信息为NULL）。

**不使用内存表**：因为它同样不支持LEFT JOIN，且SQL Server Express（工控机断网场景）不支持内存表。

**使用普通表 + 增量MERGE刷新**：

SQL Server的Buffer Pool机制会自动将频繁查询的数据页驻留在内存中。普通表配合增量刷新，90%的热数据页始终在内存，效果与内存表几乎相同，但无任何限制。

刷新策略采用事件驱动：业务代码更新任务状态时，发布缓存刷新事件，事件处理器只刷新变化的那一行数据。不变的数据行在Buffer Pool中的内存页保持有效，不会因全量刷新而作废缓存。

### 查询路由

text

```
用户请求
  ├── 列表查询（调度中心任务列表）
  │     → 走缓存表，响应毫秒级，数据时效1-5分钟
  └── 详情查询（打印单据、导出报告）
        → 走实时视图，响应百毫秒级，数据实时
```



------

## 五、技术依赖

| 组件               | NuGet包                                   | 用途                                |
| :----------------- | :---------------------------------------- | :---------------------------------- |
| EF Core 8          | `Microsoft.EntityFrameworkCore`           | 常规CRUD、多租户过滤、审计          |
| EF Core SQL Server | `Microsoft.EntityFrameworkCore.SqlServer` | SQL Server提供程序                  |
| Dapper             | `Dapper`                                  | 复杂报表SQL、单结果集存储过程       |
| System.Data        | `System.Data.SqlClient`                   | DataTable动态视图、多结果集存储过程 |

------

*文档版本：v1.0*

*文档版本：v2.0*
*对应技术栈：.NET 8 + Vue 3 + SQL Server 2019+*



## Redis 选型说明

### 一、为什么需要引入 Redis

之前的单站点模式是程序直连数据库，所有请求都打到 SQL Server。随着多租户和协作域架构的引入，系统需要处理登录会话管理、并发操作控制、高频热数据访问等需求。这些场景如果用数据库实现，会遇到以下问题：

| 场景                           | 数据库实现的代价                                    |
| :----------------------------- | :-------------------------------------------------- |
| JWT 主动失效（踢人下线）       | JWT 无状态，数据库无法主动让它失效                  |
| 并发防重复提交                 | 数据库行锁只管写入，管不住校验阶段                  |
| 每日自动编号（T-20240505-001） | COUNT 查询随着数据量增大越来越慢，并发下需表锁      |
| 高频读取热数据（如车辆状态）   | 每次解析 SQL + 生成执行计划，响应时间十几到几十毫秒 |

Redis 补充的是数据库不擅长的事情：**原子操作、主动过期、低延迟的单键读写**。

### 二、Redis 能做什么

**核心能力**：数据存在内存中，单线程处理所有命令。一个 INCR 操作不到一微秒，天然线程安全。

**在你的 ERP 中的适用场景**：

| 场景             | 解决的问题                                                   | 优先级  |
| :--------------- | :----------------------------------------------------------- | :------ |
| **登录会话管理** | JWT 无法主动失效。Redis 存 Token，管理员删键即踢人下线       | Phase 1 |
| **操作锁定**     | 两个调度员同时点"发车"。Redis SET NX 原子锁保证同一时间只有一个请求能操作 | Phase 1 |
| **分布式锁**     | 并发校验 + 写入的缺口。锁让校验和执行变成串行，第二个请求直接拒绝或等待 | Phase 1 |
| **自动编号**     | 数据库自增不能按天重置。Redis INCR 原子自增，每天一个键，凌晨自动切换，永无锁等待 | Phase 2 |
| **热数据缓存**   | 配合比模板、检验标准、系统开关——读多写极少的配置数据。存 Redis 省去 SQL 解析开销 | Phase 2 |
| **轻量消息队列** | 初期替代 RabbitMQ，处理异步报表生成、微信通知等非关键任务    | Phase 3 |
| **限流计数器**   | API 防刷、工控数据上传频率控制。INCR + EXPIRE 一行代码实现每秒限流 | Phase 3 |

### 三、Redis 不能做什么

**不适合缓存实时生产数据。** 当日任务列表、车辆状态、原材库存——这些数据读写都频繁，而且是强关联的。一个发车操作要同时更新任务表、车辆表、调度日志，如果在 Redis 里也缓存这些数据，一次写入需要刷新多个缓存键，维护成本远超收益。这些数据直接查数据库即可，SQL Server 的 Buffer Pool 已经把它们留在内存里了。

**补充而不是替代。** Redis 是缓存，不是主数据库。所有数据以数据库为准，Redis 里的是副本。Redis 宕机，系统能降级到纯数据库运行，业务不受影响。

### 四、部署方式

开发环境用 Docker 一行命令启动：

bash

```
docker run -d -p 6379:6379 redis
```



生产环境初期和 Web 应用服务器部署在同一台机器上，1 核 1G 内存实例足够支撑 30 个站点。配合 `StackExchange.Redis` 客户端，连接字符串一行配置即可使用。

# 消息队列选型说明

## 一、选型结论

**自建队列表（SQL Server）+ Redis 信号通知。**

不引入独立的 RabbitMQ 或 Kafka。消息存储在业务数据库中，Redis 仅作为轻量信号触发后台消费，避免空轮询。

## 二、适用消息类型

| 消息类型                               | 特征                               | 存储方式      |
| :------------------------------------- | :--------------------------------- | :------------ |
| **可靠消息**（监管上报、报表生成）     | 必须持久化、失败需重试、需审计追溯 | 数据库队列表  |
| **即时通知**（任务事件提醒、系统公告） | 允许丢失、消费端不在线即丢弃       | Redis Pub/Sub |

## 三、核心流程

### 3.1 消息发送

业务操作与消息写入在同一数据库事务中完成。队列表兼任前端查询的日志表——监管上报表就是上传记录表，通知队列表就是通知记录表。一张表满足后台处理和前端展示。

发送完成后尝试推送 Redis 信号（`LPUSH`），告知后台消费者有新消息。Redis 不可用时自动跳过，消费者降级为定时轮询。

### 3.2 消息消费

后台服务启动后优先阻塞等待 Redis 信号（`BLPOP`，超时 30 秒）。收到信号立即从队列表取消息处理。信号超时后主动检查一次队列表作为兜底。

消费时根据消息类型执行对应逻辑。通知类消息查询订阅表确定通知对象和渠道，逐人发送。上报类消息调用外部 API，更新处理状态。失败的消息记录错误信息并标记重试。

### 3.3 消息与订阅的关系

一条业务事件在队列表中只产生一条消息，与订阅人数无关。消费端收到消息后查询订阅表决定通知谁。一百人订阅一条事件，队列表中仍是一条记录。

### 3.4 手动重传

前端监控页面直接读取队列表展示处理状态。用户对失败记录点击重传后，后端将记录状态重置为待处理。后台服务下一轮消费自动处理，无需额外逻辑。

### 3.5 Redis 降级

Redis 不可用时消费者自动降级为 15 秒定时轮询。队列表永远在数据库中，前端可视化不受任何影响。Redis 恢复后消费者自动切回信号驱动模式。

## 四、部署

| 组件       | 用途                    | 必选 |
| :--------- | :---------------------- | :--- |
| SQL Server | 队列表存储 + 消息持久化 | 是   |
| Redis      | 消费信号通知（可选）    | 否   |

Redis 非必须。不加 Redis 时消费者用定时轮询（15-30 秒），每分钟 30 条消息的场景下完全可用。加 Redis 仅优化响应速度，不改变核心可靠性。

# [Quartz.NET](https://quartz.net/) 选型说明

## 一、选型结论

使用 [Quartz.NET](https://quartz.net/) 作为定时任务调度器。负责在指定时间触发任务，不负责耗时操作的执行。耗时操作一律委托给自建队列表 + 后台消费服务处理。

## 二、适用场景

| 场景         | 触发方式   | 例子                               |
| :----------- | :--------- | :--------------------------------- |
| 定时报表     | 按日历时间 | 每晚 22:00 生成当日生产日报        |
| 周期性检查   | 按固定间隔 | 每 30 分钟检查库存预警             |
| 定时任务触发 | 按日历时间 | 每天 8:00 触发监管数据上报任务     |
| 缓存刷新     | 按固定间隔 | 每 5 分钟增量更新生产任务缓存表    |
| 数据清理     | 按日历时间 | 每周日 4:00 归档三个月前的操作日志 |

## 三、核心原则

**Quartz 是闹钟，不是执行者。** 任务内容必须毫秒级完成——写入一条记录到队列表即结束。任何耗时操作委托给后台消费服务异步处理。这解决了上一代系统中任务卡住的问题。

## 四、定时调用外部 API 的正确方式

Quartz 不直接调用外部 API。两层分离：

**第一层：Quartz 定时触发。** 到时间后在队列表中写入一条消息，内容包含 API 的调用参数。写入成功即任务完成，耗时不超过毫秒级。即使 API 完全不可用，Quartz 任务也不会卡死。

**第二层：后台消费服务执行调用。** 从队列表取消息，发起 HTTP 请求调用外部 API。设置 30 秒超时，调用失败时标记消息状态为失败，自动重试。多次重试仍失败则记录错误并通知管理员。

队列表同时充当监控界面。前端直接查询队列表，展示每条 API 调用的状态、重试次数、错误信息。用户对失败记录点击重传后，消息状态重置为待处理，消费服务自动重新调用。

## 五、任务设计规范

所有 Quartz 任务的 Execute 方法只做以下操作之一：查询数据并写入缓存表、向队列表插入一条消息、执行一次简单的数据库写入。不做网络调用、不做文件 I/O、不做复杂计算。

## 六、失败处理

任务本身抛异常时 Quartz 记录日志，下次触发时间到达后继续尝试。对于必须成功的定时任务，通过队列表机制确保执行结果可追溯。监控每次任务的执行时长，超过正常值触发告警。

## 七、部署

开发环境使用内存存储。生产环境使用数据库持久化存储，任务配置保存在数据库中，应用重启后自动恢复。调度器与 Web 应用共享同一进程，初期不独立部署。集群部署时配置数据库锁机制，确保同一任务只在一个实例上执行。



# SignalR 选型说明

## 一、选型结论

使用 SignalR 实现服务端到客户端的实时推送。负责让前端在数据变化时自动更新，不依赖用户手动刷新。

## 二、适用场景

| 场景             | 推送内容                         | 接收方                 |
| :--------------- | :------------------------------- | :--------------------- |
| 调度中心车辆状态 | 发车、到达、签收状态变化信号     | 该站点所有在线调度员   |
| 新任务通知       | 新任务创建信号                   | 相关站点在线操作员     |
| 异步任务完成     | 报表生成完成、监管上报完成       | 提交请求的用户         |
| 操作冲突提示     | 他人已修改当前记录               | 正在查看同一记录的用户 |
| 系统告警         | 库存预警、设备故障、上报连续失败 | 相关站点在线管理员     |

## 三、核心原则

**推送信号，不推送数据。** 服务端只推送"什么数据变了"，前端收到后调用已有 API 重新获取。推送保持轻量，数据获取重用已有接口。

**用户上线时加入对应的组。** 按搅拌站分组、按协作域分组。推送时指定组，组内所有在线用户收到信号。用户不在线时收不到，上线后查记录即可，不需要离线消息存储。

## 四、与已有组件的关系

队列表保证消息可靠处理和状态追踪。SignalR 保证处理完成后前端能立即感知。队列表不关心谁在线，SignalR 不关心消息是否持久化。两者各司其职，依次配合。

> [!NOTE]
>
> JWT
>
> **Element Plus**
>
> FastReport.OpenSource

**Pinia**

**Vue Router 4**  

**Axios**

**Vxe-Table**

**ECharts 5**

uni-app