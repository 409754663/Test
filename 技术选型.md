## 混凝土ERP系统架构选型方案

### 一、总体架构概览

采用**分层、可演进的单体外加模块化设计**。初期作为单体应用部署，但内部模块高度解耦，未来可平滑拆分为微服务。

text

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│   Web端 (PC)  │  移动端 (H5/小程序)  │  工控机终端          │
└────────────────────────┬────────────────────────────────────┘
                         │  HTTPS / WSS
┌────────────────────────▼────────────────────────────────────┐
│                      接入层 (Nginx)                          │
│         反向代理  │  负载均衡  │  SSL卸载  │  限流           │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    API 网关层                                │
│     认证鉴权  │  路由转发  │  协议转换  │  日志记录         │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    应用服务层                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ 经营合同  │ │ 生产调度  │ │ 技术质量  │ │ 物资库存     │  │
│  │ 服务     │ │ 服务     │ │ 服务     │ │ 服务         │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ 结算财务  │ │ 设备管理  │ │ 报表服务  │ │ 消息通知     │  │
│  │ 服务     │ │ 服务     │ │          │ │ 服务         │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              系统基础服务层                            │  │
│  │  租户管理 │ 用户认证 │ 权限管理 │ 日志审计 │ 定时任务  │  │
│  │  文件存储 │ 消息队列 │ 配置中心 │ 租户脚本引擎        │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    数据层                                    │
│  ┌────────────┐  ┌────────────┐  ┌────────────────────┐    │
│  │ SQL Server │  │   Redis    │  │  MinIO / 阿里云OSS │    │
│  │ (主数据库) │  │ (缓存/会话) │  │ (文件/报告存储)    │    │
│  └────────────┘  └────────────┘  └────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    边缘层 (站内工控机)                        │
│  数据网关 │ PLC通讯 │ 本地缓存 │ 断网续传 │ 脱机生产保障    │
└─────────────────────────────────────────────────────────────┘
```
------

### 二、前端技术栈选型

| 选型项         | 推荐方案           | 备选方案                     | 选型理由                                                     |
| :------------- | :----------------- | :--------------------------- | :----------------------------------------------------------- |
| **框架**       | **Vue 3**          | React 18                     | Vue 3学习曲线平缓，适合中小团队快速开发。组合式API（Composition API）能很好地组织复杂逻辑，与你的“基础模块→集成中心”思路天然契合。 |
| **构建工具**   | **Vite**           | Webpack 5                    | 开发服务器秒级启动，热更新极快，显著提升开发体验。           |
| **UI组件库**   | **Element Plus**   | Ant Design Vue, Naive UI     | 中文社区活跃，文档齐全，组件覆盖面广（表格、表单、弹窗等ERP常用组件均成熟），支持你的“统一风格”需求。 |
| **状态管理**   | **Pinia**          | Vuex 4                       | Vue 3官方推荐，TypeScript支持好，模块化设计，适合管理多租户、多岗位的复杂状态。 |
| **路由**       | **Vue Router 4**   | -                            | Vue 3标准路由方案。                                          |
| **HTTP客户端** | **Axios**          | -                            | 成熟的请求库，拦截器机制适合封装全局认证、错误处理、租户头注入。 |
| **表格组件**   | **Vxe-Table**      | AG Grid (社区版)             | 专门的强化表格组件。搅拌站ERP里列表页几乎是一切，复杂的列配置、数据导出、单元格编辑，Vxe-Table都能低成本实现。 |
| **图表库**     | **ECharts 5**      | AntV G2                      | 百度开源，案例和文档丰富，适合报表看板的图表需求。支持数据下钻交互。 |
| **微前端**     | **Qiankun** (预留) | Micro-app, Module Federation | 为未来的租户自定义页面做准备。初期可不用，但架构上预留子应用加载能力。 |
| **移动端**     | **Uni-app**        | Taro                         | 一套代码编译到H5、微信小程序、APP。搅拌站老板看报表、调度收通知，移动端是刚需。 |

------

### 三、后端技术栈选型

你的团队SQL Server能力很强，后端语言的选择需要与之高效配合。

| 选型项           | 推荐方案                                                     | 备选方案                                       | 选型理由                                                     |
| :--------------- | :----------------------------------------------------------- | :--------------------------------------------- | :----------------------------------------------------------- |
| **框架**         | **.NET 8**                                                   | Java 17 (Spring Boot 3)                        | **强烈推荐**。 1. **与SQL Server同属微软生态**，配合天衣无缝，Entity Framework Core + SQL Server的集成体验是业界最好的。 2. 你的团队有SQL Server能力，对C#的接纳成本会很低。 3. .NET 8性能极强，跨平台，中小团队生产力极高。 4. 与工控机（通常Windows环境）通讯有天然优势。 |
| **ORM**          | **Entity Framework Core 8**                                  | Dapper                                         | **EF Core用于80%的常规业务逻辑**，开发效率极高，LINQ查询比写SQL快得多。 **Dapper用于20%的复杂报表SQL**，发挥团队原生SQL能力，直接跑高性能查询。 两者可共存。 |
| **认证与授权**   | **[ASP.NET](https://asp.net/) Core Identity + IdentityServer** | 自研JWT体系                                    | 成熟的认证框架，支持角色、声明、策略，与多租户权限体系易集成。 |
| **多租户支持**   | **EF Core 全局查询筛选器**                                   | 数据库RLS                                      | EF Core的`HasQueryFilter(b => b.TenantId == currentTenantId)`在ORM层自动注入`tenant_id`，开发时不用手动加，比RLS更温和且易调试。数据库RLS作为防线兜底。 |
| **API文档**      | **Swagger / Swashbuckle**                                    | -                                              | 自动生成交互式API文档，前后端联调必备。                      |
| **定时任务**     | **[Quartz.NET](https://quartz.net/)**                        | Hangfire                                       | 成熟的任务调度库，支持Cron表达式，满足“定时备份”、“自动报表生成”、“库存预警”等需求。 |
| **消息队列**     | **RabbitMQ** (初期可用Redis Pub/Sub)                         | Kafka                                          | 初期用户少时，可用Redis轻量处理“消息通知”、“异步报表生成”，无额外部署成本。规模上来后切换到RabbitMQ，两者接口可通过抽象层隔离。 |
| **租户脚本引擎** | **Jint** (JavaScript引擎)                                    | ClearScript, [Python.NET](https://python.net/) | 用JavaScript作为租户自定义脚本语言，与前端的JS技术栈统一，降低学习成本。Jint是纯C#实现，安全可控。 |

------

### 四、数据库选型与设计原则

| 选型项            | 推荐方案                              | 说明                                                         |
| :---------------- | :------------------------------------ | :----------------------------------------------------------- |
| **主数据库**      | **SQL Server 2019+**                  | 必须。团队能力所在。使用行级安全性（RLS）作为数据隔离的最终防线。使用列存储索引优化报表。考虑使用**Always On可用性组**实现读写分离和故障转移。 |
| **缓存与Session** | **Redis**                             | 存储用户登录会话、常用配置、实时车辆状态等热数据，减少数据库压力。 |
| **文件存储**      | **MinIO** (自建) / **阿里云OSS** (云) | 存储合同扫描件、签字图片、试块照片、生成的PDF报告等静态文件。MinIO兼容S3协议，部署简单，可本地也可云上。 |

**数据库设计核心约定**（所有表）：

- `TenantId`：int，租户隔离主键，**所有业务表必有**。
- `RegionCode`：varchar(10)，地区编码，预留未来区域化管理。
- 使用表前缀区分模块，如`Ctr_`、`Prd_`、`Tech_`等。
- 所有表包含`CreatedAt`, `UpdatedAt`, `CreatedBy`, `UpdatedBy`审计字段。
- 所有删除使用**软删除**（`IsDeleted`位字段），配合EF Core全局筛选器自动过滤。

------

### 五、接口层设计规范

| 选型项       | 推荐方案               | 说明                                                         |
| :----------- | :--------------------- | :----------------------------------------------------------- |
| **API风格**  | **RESTful API**        | 主流、清晰、易理解。用于前后端交互、第三方系统对接。         |
| **数据格式** | **JSON**               | 标准格式。                                                   |
| **版本管理** | `/api/v1/` URL前缀     | 为未来升级预留。                                             |
| **错误处理** | 统一错误码体系         | 定义全局错误码枚举（如`10001=租户不存在`, `20001=配比未授权`），所有异常通过全局异常过滤器处理，返回`{ code, message, data }`标准格式。 |
| **实时通讯** | **SignalR**            | [ASP.NET](https://asp.net/) Core原生支持WebSocket，可用于“调度中心”的实时车辆状态、新任务提醒、消息推送，比轮询高效得多。 |
| **设备接口** | **独立的协议转换网关** | 工控机、地磅等硬件接口不直接开放HTTP。专门写一个**边缘网关服务**，负责串口/PLC协议采集，再通过MQTT或加密HTTP上传到云端。 |

------

### 六、报表与打印方案

| 选型项       | 推荐方案                                               | 说明                                                         |
| :----------- | :----------------------------------------------------- | :----------------------------------------------------------- |
| **报表引擎** | **FastReport.OpenSource ** .NET生态老牌报表，开源免费，对SQL Server直连友好。 无报表设计器，需编码或者预置.frx模板 |
------

### 七、部署架构建议

| 环境              | 说明                                                         |
| :---------------- | :----------------------------------------------------------- |
| **开发环境**      | 开发人员本地。SQL Server可用Docker部署（微软已提供官方镜像），Redis用Docker。 |
| **测试环境**      | 一台低配云服务器，模拟生产环境。                             |
| **生产环境-云端** | **应用服务器**：Windows Server / Linux均可（.NET跨平台）。初期一台4核8G云服务器即可承载30个站。 **数据库服务器**：独立的一台云服务器跑SQL Server，规格视数据量而定。 **文件服务器**：MinIO单实例或OSS。 |
| **生产环境-站内** | 一台工控机，安装Windows（大多数搅拌站PLC配套软件要求）。部署你的边缘网关服务，本地SQL Server Express（免费版）做断网缓存。 |

------

### 八、开发协作工具建议

| 工具                      | 用途                                                         |
| :------------------------ | :----------------------------------------------------------- |
| **Azure DevOps / GitLab** | 源码管理、CI/CD流水线、需求管理。DevOps对.NET生态有天然支持。 |
| **Docker**                | 统一开发环境，尤其是SQL Server和Redis的本地部署。            |
| **Swagger / Apifox**      | API文档与前后端联调。                                        |